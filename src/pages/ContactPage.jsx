import Layout from '../components/Layout';

const ContactPage = () => {
  return (
    <Layout>
      <section className="page-section" id="contact">
        <h1 className="page-section__title">Contact</h1>
        <p className="page-section__lead">
          Reach out via the channels listed in the footer, or use this page as a dedicated contact hub in a future update.
        </p>
      </section>
    </Layout>
  );
};

export default ContactPage;
